# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3

# HTTP client
httpx==0.27.2

# DB
psycopg2-binary==2.9.10

# ── LangGraph + LangChain ─────────────────────────────────────────────────────
langgraph==0.2.56
langchain==0.3.12
langchain-core==0.3.25
langchain-openai==0.2.12
langchain-community==0.3.12

# ── Graph Memory (Zep Graphiti) ───────────────────────────────────────────────
graphiti-core==0.3.21

# ── Neo4j driver ──────────────────────────────────────────────────────────────
neo4j==5.26.0

# ── Vector Store (Qdrant) ─────────────────────────────────────────────────────
qdrant-client==1.13.0

# ── Local embeddings ──────────────────────────────────────────────────────────
sentence-transformers==3.3.1
torch==2.5.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# ── OpenAI SDK (used by langchain-openai + graphiti) ─────────────────────────
openai==1.58.1

# Redis (short-term memory)
redis==5.2.1

# NLP / analysis (legacy analyzers)
jieba==0.42.1
numpy>=1.24,<2.0
scipy>=1.11

# Document parsers (legacy analyze endpoint)
pdfminer.six==20221105

# Async HTTP (used internally by novel-downloader search)
aiohttp>=3.9.0

# Novel download / search (multi-site: 笔趣阁, 起点 etc.)
# Installed from the git submodule at python-sidecar/novel-downloader/
# (see Dockerfile).  For local dev: pip install python-sidecar/novel-downloader
# After cloning: git submodule update --init --recursive
